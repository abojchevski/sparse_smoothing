from setuptools import setup

setup(name='sparse_smoothing',
      version='0.1.0',
      description='Efficient Robustness Certificates for Discrete Data: Sparsity-Aware Randomized Smoothing for Graphs, Images and More',
      packages=['sparse_smoothing'],
      zip_safe=False)
